{% extends 'base.html.twig' %}

{% block title %}Cryptomonnaies{% endblock %}

{% block body %}

    <div class="example-wrapper">
        <h1>{{'crypto.list'|trans}}</h1>
        <table class="table text-center">
            <thead>
            <tr>
                <th class="active text-center" scope="col">Nom</th>
                <th class="active text-center" scope="col">Créateur</th>
                <th class="active text-center" scope="col">Quantité</th>
                <th class="active text-center" scope="col">J'aime</th>
                {% if app.user %}
                    <th class="active text-center" scope="col">Favoris</th>
                {% endif %}
            </tr>
            </thead>
            <tbody>
            {% for crypto in cryptos %}
                <tr>
                    <td><a href="{{ path('crypto_info', {nom: crypto.nom}) }}">{{ crypto.nom }}</a></td>
                    <td>{{ crypto.createur }}</td>
                    <td>{{ crypto.quantite }}</td>
                    <td>{{ crypto.nbLikes }}</td>
                    {% if app.user %}
                        <td>⭐</td>
                    {% endif %}
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>

{% endblock %}